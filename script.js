const form=document.getElementById("trialForm"),status=document.getElementById("status");form.addEventListener("submit",e=>{e.preventDefault();status.textContent="Demo request received ✓ — the submission connection is next.";setTimeout(()=>status.textContent="Demo form for V3 — we'll connect submissions in the next build step.",4000)});